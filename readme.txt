To compile, use "make"
To clean the execution file, use "make clean"
To execute, use "./HW1"
When executed, it will print the information required on terminal.
Eigen package is included to satisfy matrix calculation.

